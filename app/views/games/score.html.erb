<h1>Result</h1>

<% if @word.difference(@letters).length > 0 %>
  <p>
    Sorry but <strong><%= @word.join.upcase %></strong> can't be built out of <%= @letters.join(" ").upcase %>
  </p>
<% elsif !@is_valid %>
  <p>
    Sorry but <strong><%= @word.join.upcase %></strong> does not seems to be a valid English word...
  </p>
<% else %>
  <p>
    <strong>Congratulations!</strong> <%= @word.join.upcase %>  is a valid English word!
  </p>
<% end %>

<%= link_to "Play again", new_path %>
